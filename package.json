{
  "name": "@springmath/hubspot-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "A restricted, pipeline-and-brand-bound HubSpot ticket broker",
  "type": "module",
  "engines": { "node": ">=24 <27" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/config.js && node --check src/broker.js && node --check src/server.js && node --check src/replies.js && node --check src/send-reservations.js && node --check src/errors.js",
    "token": "node scripts/generate-token.js"
  }
}
