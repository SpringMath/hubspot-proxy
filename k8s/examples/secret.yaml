# DOCUMENTATION ONLY. Not included in kustomization.yaml; do not commit a filled
# version or apply this placeholder. Provision a real Secret out-of-band.
apiVersion: v1
kind: Secret
metadata:
  name: hubspot-proxy-secrets
  namespace: hubspot-proxy-demo
type: Opaque
stringData:
  HUBSPOT_ACCESS_TOKEN: "REPLACE_VIA_SECRET_MANAGER"
  BROKER_TOKEN_SHA256: "REPLACE_WITH_SHA256_OF_RANDOM_BROKER_BEARER"
